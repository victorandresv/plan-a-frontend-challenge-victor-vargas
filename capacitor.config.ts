import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Plan a Frontend Challenge Victor Vargas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
